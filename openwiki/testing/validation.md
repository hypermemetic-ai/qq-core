---
title: Validation
---

# Validation

Run `npm test` in each changed repository. Core's geometry suite checks catalog,
launcher, optional host binding, and repository-root session assumptions.
`qq-workflows` tests exact cwd mapping and clone-only capsules with an internal
`.git`. The core host boot suite exercises missing optional siblings.
